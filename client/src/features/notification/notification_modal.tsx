import {NotificationModel} from "../../model/notification";

interface NotificationModalProps {
  notificationModels: NotificationModel[]
}

export default function NotificationModal({ notificationModels } : NotificationModalProps) {
  
}